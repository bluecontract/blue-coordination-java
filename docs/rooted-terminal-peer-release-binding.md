# Active witness-selection release binding

This binding-only successor starts from algorithm candidate `bbd06a0b5ef1f2c3db021f02406b892c2676ce86`. The original algorithm worktree and its diagnostic evidence are unchanged.

Language generation from `90874a036dd1041336354b3141ac9fad56e0f672` produced Contracts release identity `sha256:433838a407fb8dffab7ef4f84cb47a2a0cf95595ad0632bbb54462f266a1accc`. This source follows the witness-selection implementation `00b745fea8b83eca4a2cfc7558048cf3ecff600d`. Parent review of the complete 383-file generated package reported only the release manifest changed: two existing implementation hashes, one newly inventoried implementation source, and the derived release identity. The Contracts specification remains `sha256:5cc29e91cd8d4aa4d3dca98214da5ceb49b2daa82554ac561260bd98ce5063b8`; the fixture package remains `sha256:9323cd0b2b4202c08d8165a99102aa6d8a52f3e718fc33647e34ba211859a60f`.

Only the active SDK `contracts-1.0-release.properties` release field and its exact assertion in `SdkCoreSeamsTest` are rebound. The Blue Language specification, Contracts specification, fixture identity, gas manifest, finalizer, and verifier identities stay unchanged. Historical diagnostics, fixture packages, and old reviewed release records retain their original bindings; no generic release fallback or compatibility override is introduced. No algorithm or other test assertion changes are included.

The reviewed source tree is `592a68f6829494f71b1f3add4afff360b57f5361`, source epoch `1789189748`. Its generated manifest is `/Users/kamil/Documents/Projects/Blue/legal-detached-retarget-evidence.fKnxrU/witness-selection-generation.VOkg0r/release/conformance/contracts/release-manifest.yaml`, SHA-256 `8e15b0487bc8c464b6f072eadcf1ad2a642744aded957c5e4eb9aaf7f1f23500`. This checksum authenticates the generated resource file only.

The identity above is a **generated Contracts release identity, not an artifact SHA**. Sealed Language/BEX/Catalog artifact identities and repository-manifest hashes are pending the parent's export and verification. No compilation, test, export, full-clean qualification, performance acceptance, or merge readiness is claimed by this binding commit. Run the candidate tests only against the subsequently verified exact artifact tuple; do not treat an older installed Language artifact as implementing `rootedWitnessSelection`.

## Test-compilation correction

The qualification attempt on frozen binding source `0f5333c8a749310cd2a968c39064ac542a066815` stopped at `compileTestJava`; zero tests executed. The late-gas terminal probe used a diagnostic accessor on `RootedCalculationFixture`, which does not expose it. This successor obtains the same latest PROCESS evidence through the existing `CoordinationTestControl.attach(rawEngine)` accessor and corrects the class description to acknowledge its terminal safety probes. Every assertion is unchanged. Production sources and active release bindings are byte-identical to `0f5333c8a749310cd2a968c39064ac542a066815`; the frozen export source remains untouched. Compilation and test execution of this correction are pending the parent's gate.
