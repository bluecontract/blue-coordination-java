# Reconciled Language binding and topology evidence

## Baseline-first successor: C03 deferred

The next candidate excludes the whole nested/contextual identity family and
restores pinned merged Language Core identity/minimizer behavior. The historical
generation02 bindings below remain in the current Coordination source; they must
not be mistaken for the forthcoming reduced-source tuple. Coordination contains
no `specification-set.json`: Language owns that manifest and must retain its
baseline Language row `77b48506ff7b5ddbab26b98ce9e060e943cb3babf1e6f5511085bbb3c31c4144`
(217,483 bytes), not the deferred contextual specification `ebbaf3dc...`.

Authenticate all seven profile fields from the new owning JARs and sealed
manifest. With baseline specifications and tariffs retained, those three fields
are expected to stay unchanged; release/fixture and cyclic source-role identities
must be measured and rebound, never guessed. Rebuild BEX/Catalog exports against
the same new Language manifest even if their source commits do not change.
Removing the unpublished three-argument minimizer requires no Coordination code
adapter. Retain the contribution/witness APIs and merged processing-event APIs.

There is no profile generator: patch the authenticated properties and exact
`SdkCoreSeamsTest` release/fixture literals, changing
`ROOTED_CONTRACTS_SPECIFICATION` only if its independently verified value changes.
Commit clean inputs before the filtered topology exporter below. Review all 32
records, preserving numeric gas, history/state, event/work order, ownership,
failure and rollback assertions; no manual topology rehash is permitted. Commit
the reviewed generated JSON/Markdown before export and refresh the exact
Coordination development version. Keep all RC10 published defaults unchanged.

Before topology/full gates, use the sealed dependency arguments with the focused
`SdkCoreSeamsTest`, complete `SdkNamedComputeDefinitionTest`,
`ComputeProgramNormalizerTest`, `RootedNestedDocumentValueIdentityTest`,
`ScopedProcessorExecutionContextBexDocumentViewTest`,
`RootedSourcePrerequisiteObservationTest`, and the two `processor.bex` owners
`ProcessingEventIdentityEvidenceTest` / `BexHandlerEventBindingTest`.
The two C03-only parameterized SDK cases are deferred, not disabled; every
pre-existing owner assertion remains. Exact runtime discovery is authoritative.
Final Java 17/21 `releaseCheck`, extracted-source, exported-consumer and complete
MyOS gates remain required on one frozen tuple. No new execution is claimed here.

## Historical generation02 procedure status

This procedure was inspected against pinned Coordination RC10 plus the current
source reconciliation. The reviewed generation02 input bindings below have been
applied; artifact authentication, regeneration and qualification commands have
**not been executed**. The parent workstream owns the isolated artifact
repositories, Gradle home, source freeze and heavy run lane.
The [disposition](baseline-reconciliation-disposition.md) and
[historical correction record](rooted-baseline-release-fixes.md) define scope.

## Applied generation02 input binding

The parent approved the staged Language package at
`baseline-reconciliation-evidence-20260915/runs/language-regeneration-02/release`
after static and identity review. Its
`conformance/contracts/release-manifest.yaml` file SHA-256 is
`b19f757063c35306327d142e2c586e99f6c1ad32c863ec970e83f6bd625331ec`;
this file digest is not the semantic `contractsRelease` identity.

| Property | Applied identity | Change from pinned RC10 profile |
| --- | --- | --- |
| `blueLanguageSpecification` | `sha256:77b48506ff7b5ddbab26b98ce9e060e943cb3babf1e6f5511085bbb3c31c4144` | Unchanged, verified. |
| `contractsSpecification` | `sha256:5cc29e91cd8d4aa4d3dca98214da5ceb49b2daa82554ac561260bd98ce5063b8` | Changed. |
| `contractsRelease` | `sha256:06ca8273a28f9fb0298ec6aae672c2558b613582b3e61d4c651e86399fe67631` | Changed. |
| `fixturePackage` | `sha256:7bd3a699d1649bd20c715ef001d61a39547942fa7db3ab65c434a480e025db47` | Changed. |
| `gasManifest` | `sha256:03219c42eb3696ef8727fe8ae226c8a5eb4a6126859ba744f571d892c409626a` | Unchanged, verified. |
| `cyclicFinalizer` | `sha256:d4f8934f82e2330a165a612e4d9d72e0ceff1c7c0df37b0d08772e66629344c7` | Changed. |
| `cyclicProofVerifier` | `sha256:bd14a60f67cbc9a19fadc4b0d5a4adc8ed7578151f0c14c2591835dd8b9f73a0` | Changed. |

`ROOTED_CONTRACTS_SPECIFICATION` uses the same reviewed specification identity;
`SdkCoreSeamsTest` now asserts the reviewed release and fixture identities instead
of frozen-donor literals. The existing owning-JAR byte, runtime constant,
rooted-profile and wrong-profile negative assertions are unchanged.

The seven fields were checked against staged specification bytes, release and
fixture manifests, and current Language runtime/GasSchedule constants. This
source input binding remains pending authentication against the parent's sealed
post-commit artifact manifest and owning JARs. RC10 published dependency defaults,
version metadata and release notes are unchanged. The active topology artifacts
still contain the prior inputs and outcomes: this changed runtime requires full
maintained regeneration and review, not manual rehashing or a metadata-only
substitution. No generated topology bytes have been changed.

## Bind authenticated identities before configuration

The authoritative input is the newly verified Language artifact repository's
`artifact-manifest.json`, together with its generated Contracts release package
and exact staged JAR bytes. Do not use frozen d220/e28 values, a checked-in stale
generated package or similarly named runtime registry fixture identities.

| Coordination file / value | Authoritative new source |
| --- | --- |
| `src/main/resources/blue/coordination/sdk/contracts-1.0-release.properties`: `contractsSpecification` | Language artifact manifest `contractsSpecificationIdentity`, independently matching the exact Contracts JAR specification resource. |
| Same properties file: `contractsRelease` | Manifest `contractsReleaseIdentity`; this is a semantic generated release identity, not the artifact-manifest hash or a JAR digest. |
| Same properties file: `fixturePackage` | Manifest `contractsFixturePackageIdentity`, the aggregate Contracts release/closure fixture identity. |
| Same properties file: `blueLanguageSpecification`, `gasManifest`, `cyclicFinalizer`, `cyclicProofVerifier` | Corresponding verified release package/specification/runtime constants from the selected artifacts. Preserve values only after checking they remain equal. |
| `src/main/java/blue/coordination/internal/ContractsClosureProfile.java`: `ROOTED_CONTRACTS_SPECIFICATION` | The same new `contractsSpecificationIdentity`, so the intended exact reviewed specification selects rooted processing. |
| `src/test/java/blue/coordination/internal/SdkCoreSeamsTest.java`: exact release/fixture assertions | The new manifest's release and aggregate fixture identities. Its owning-JAR specification hashes and gas/finalizer/verifier assertions remain intact. |

`BundledContracts10Release` reads all seven properties. `SdkCoreSeamsTest`
authenticates specification bytes from the owning JAR rather than classpath
shadow resources, verifies runtime identities and checks exact profile selection
including its wrong-profile negative. Preserve that entire validation boundary.

`gradle/development-catalog.gradle` verifies the profile's release/fixture values
against the selected Language manifest **during settings evaluation**. Rebind
those fields before any development-lane Gradle invocation; do not bypass the
guard to compile against mismatched artifacts. No profile-update Gradle task
exists: update the exact source fields with a reviewed patch.

## Exact development properties

Use the parent's exact argument set for every invocation below. The shell array
`baseline_dependency_args` denotes that already resolved argument set, not a
second dependency selection. It must contain:

- `-PblueDependencyMode=immutable-development-contracts`;
- `blueContractsVersion`, `blueContractsRepository`,
  `blueContractsManifestSha256`, `blueContractsSourceCommit`;
- `blueBexVersion`, `blueBexRepository`, `blueBexManifestSha256`,
  `blueBexSourceCommit`;
- `blueRepositoryVersion`, `blueRepositoryRepository`,
  `blueRepositoryManifestSha256`, `blueRepositorySourceCommit` for Catalog;
- `blueDevelopmentVersion=3.0.0-dev.<exact-clean-Coordination-commit>`.

All property names above take `-Pname=value`. Repositories are absolute,
immutable, baseline-owned paths outside the source tree; every source/manifest
value comes from the selected sealed tuple. The actual Catalog group/artifact is
`blue.repo:blue-repo-java`; its property prefix is `blueRepository`, not
`blueCatalog`. Its manifest must bind the same Language version, source commit,
tree and repository-manifest identity. BEX has the corresponding Language binding.
Development versions and source manifests require the maintained clean-source
contract; the parent freezes a concrete source before qualification and refreshes
the Coordination version if generated evidence creates a subsequent commit.

The maintained preflight/report command is:

```bash
./gradlew --no-daemon --max-workers=1 \
  --gradle-user-home "$BASELINE_GRADLE_USER_HOME" \
  verifyActiveDependencyLane dependencyPreflight writeDevelopmentResolvedDependencies \
  "${baseline_dependency_args[@]}"
```

This writes `build/reports/development-stage/resolved-dependencies.json` and
verifies the exact graph. The parent should retain command, environment, source
identity, manifests and output hashes in its candidate receipt. Development
verification does not qualify published RC10 defaults.

## Regenerate active topology artifacts with the maintained exporter

The active paths are:

- `stabilization/rooted-checkpoint/retained-topology-identities/full-lifecycle-admission-identities.json`;
- `stabilization/rooted-checkpoint/retained-topology-identities/full-lifecycle-admission-identities.md`.

They are not under a `verification/cyclic-topology` directory. Their seven
`inputs` fields come directly from `BundledContracts10Release.manifest()`.
`CyclicTopologyIdentityEvidenceTest` is the maintained exporter; no external
substitution script or new classifier is needed. For an explicit regeneration:

```bash
BLUE_CYCLIC_TOPOLOGY_IDENTITY_ARTIFACT_MODE=WRITE \
./gradlew --no-daemon --no-build-cache --max-workers=1 \
  --gradle-user-home "$BASELINE_GRADLE_USER_HOME" \
  test --tests blue.coordination.internal.CyclicTopologyIdentityEvidenceTest \
  -PtestJavaVersion=17 -PtestMaxParallelForks=1 \
  "${baseline_dependency_args[@]}"
```

The filtered exporter directly runs the original 20 contributor methods and
validates all 32 scenario records and mandatory repeat counts. This is a heavy
generation run, not permission for another agent to start one independently.
Review the complete generated diff: specification/release changes can affect
exact identities, but no history order, ownership, event, gas, failure or rollback
oracle may be weakened to accept a mismatch. Preserve historical release notes
and explicitly legacy fixture profiles. The frozen donor's outcome bytes are
reference evidence, not replacement outputs for this new runtime.

The historical workflow permits an exact input-header patch only when inspection
proves a change is solely release input metadata and every other byte remains
valid, still subject to the maintained exporter/comparator. Generation02 changes
the runtime and is not that case: use the full exporter above.

## Verify with write mode absent

After reviewing and freezing the regenerated artifacts, the parent runs the
complete candidate gate, beginning with Java 17:

```bash
env -u BLUE_CYCLIC_TOPOLOGY_IDENTITY_ARTIFACT_MODE \
./gradlew --no-daemon --no-build-cache --max-workers=1 \
  --gradle-user-home "$BASELINE_GRADLE_USER_HOME" \
  clean releaseCheck dependencyPreflight \
  -PtestJavaVersion=17 -PtestMaxParallelForks=1 \
  "${baseline_dependency_args[@]}"
```

The same final candidate also requires Java 21 and the parent-owned exported
consumer and complete MyOS gates. The parent may use the supported resource
limits it has coordinated; changing worker count does not remove required scope.

In complete test mode, the 20 original contributors record distinct fragments
under `build/rooted-evidence/topology-fragments/`. The existing
`verifyCyclicTopologyIdentityEvidence` finalizer combines them once in canonical
order and compares committed JSON/Markdown exactly; it must not be excluded.
Comparison reports are under `build/reports/topology-identity-evidence/`.
`verifyCheckTestExecutionScope` and `verifyReleaseTestExecutionScope` require
actual complete JUnit discovery/results and reject filtering, skips and missing
comparison. A filtered generation run does not satisfy those gates.

The input binding is applied, but owning-JAR/sealed-export authentication,
generated topology evidence and all qualification runs remain pending. This
document does not claim acceptance of the candidate or published RC10 defaults.
