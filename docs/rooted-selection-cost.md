# Bounded rooted input selection

A packaged saved-A5 join in a twelve-document realm exceeded its unchanged30-second catch-up bound, committing three of five imports. The serial reproduction had the same result. Diagnostic continuation completed the remaining imports, next LIVE entry, and exact fresh-JVM relocation/restart of all12documents and87command records. This is evidence of finite but excessive work, not a passed deadline.

The selector rebuilt the same exact selected view once per retained journal entry. The focused RootedSelectionCostTest reproduces13routing-surface compilations when12entries have already been processed. It requires one capture and still verifies the next exact input, no publication during selection, committed progress and a subsequently appended input.

The correction retains the capture only inside a single synchronized nextRootLiveInput call. Each candidate still performs route selection, exact checkpoint eligibility, its own cause and terminal evidence lookup. The next call captures fresh owner/head/graph fences. Semantic PROCESS work and gas remain unchanged.

Thirteen focused SDK tests passed, including exact gas boundaries/cache schedules, frozen witness validation, historical interleaving, exact-target routing and global-driver ordering. Complete current artifact and release gates remain required.
