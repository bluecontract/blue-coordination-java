# Dynamic ring/chord terminal-successor reproduction

Status: the genuine SDK defect was reproduced on unchanged Coordination d0382bbc95a1ba3fe77bd8600fa610ee3041220d. The original five-input reproduction then passed on the consolidated candidate containing this private correction and the separately tracked Commerce, FULL_HISTORY and eligibility-memo corrections: **1 test / 1 PASS**, 43.835 seconds JUnit suite time, 53 seconds build time. This is not full MyOS acceptance or isolated qualification of every combined change. Both methods and the explicit three-root READY/restart checks subsequently passed in [the source-stable 44-case combined gate](rooted-history-combined-qualification.md).

The first isolation run failed in its helper before the target: **1 test / 1 failure**, 3.668 seconds. After the first APPLIED entry it rejected a legitimate empty STALE transport acknowledgement because the helper expected only NO_MATCH. The original source and XML are preserved at `/Users/kamil/Documents/Projects/Blue/rooted-ring-chord-sdk-red.Jw4LgZ`; XML SHA-256 `0bc7bbced48db911a677727e9ab6a9934ffbfa8b8e1f7450fbf98da31cfae3cc`. This is a fixture failure, not the successor defect.

The corrected helper follows `SdkDrainResultMapper.mapEntry/diagnoseZeroAttempts`: a targeted exact-version input with no remaining PROCESS attempts can be STALE_TARGET_DOCUMENT after its own successful transition changed the head. It requires the earlier APPLIED result and its still-retained committed closure, zero entry/total gas and transitions, no closures/events/managed or local attempts, and unchanged complete receipt-identity histories and exact heads for all roots. It accepts neither a first STALE result nor a nonempty later attempt under this rule. The five exact input IDs and timestamps remain unchanged.

## Actual downstream failure

The frozen MyOS `baseline-followup-shared-source-and-oracles-02` executes the maintained `docs/tutorial/test_graphs.py` ring/chord fixture with the sealed final Language 2ce / Coordination d038 tuple. Its Java child PID 16367 naturally fails the unchanged 600-second READY assertion after the fifth Timeline operation. The fatal SDK stack occurs much earlier, at `ManagedEpochInvocationCapturer.capture:301`: `Numbered work omitted its frozen terminal representation successor`.

The saved-authored sequence is A attaches B, B attaches C, C attaches A, A emits a token addressed to C with next=B, then A adds a second route `/peers/c` to saved-authored C. All five exact entries are APPLIED. The last attachment consumes C0 through C6, then the actual C7 work fails before PROCESS. A is committed 13 / READY 5, B 12 / 4, C 7 / 7. The C7 numbered receipt ends at `97DidZEwqSSKfVeRJcWu5j3hk4hV95ddFf7UYGuMeVaH#1`, while its current same-epoch representation is `33waGPERr2gfJYpUMipUiQefDhEFG2jjGvZwWMeYUXsn#0`.

The raw diagnostic, exact authored sources, complete five Timeline entries, commands, plans, work, barrier, final Python failure and uniquely owned host log are retained outside the source tree at `rooted-baseline-release-review.VamIZv/ring-chord-live.tci3yd`. Its checked SHA256SUMS digest is `c0863e2feb7ba3dcfc3e1c6b49c065b3746553a0c53af1a18f35bd71976c66e6`. The parent retains the complete HTTP trace in the recursive batch archive.

## Bounded SDK isolation

`RootedRingChordSuccessorReproductionTest` reuses the maintained node template. Changing only its existing channel key from `owner` to `ownerChannel` and substituting the observed namespace produces the exact captured authored source bytes. The test asserts all three authored identities and all five Timeline entry identities plus their exact logical-clock timestamps and predecessor chain. A different generated input fails before any attribution to the successor defect.

All operations use the actual public SDK. Each turn follows the actual audited canonical lane, including root-local work if selected. No SQL rows, receipts, source births, catch-up work, or authority are fabricated. The 64-turn loop is a finite diagnostic bound, not an execution-gas allowance or a replacement for the original HTTP deadline. Immutable receipt prefixes and complete source event occurrence data remain checked. The test stops at the first failed exact turn, rather than waiting 600 seconds for an already failed command.

The test-only `RootedTerminalSuccessorProbe` calls the two actual read-only source selectors at the C7 boundary: planner `forConsumer` versus invocation `forCapturedRoot`. It prints the actual canonical work identity, frozen boundary, selected source state, and each successor cause identity. It does not replace or repair selected work. Actual execution still enters the original SDK capturer and its unchanged omission/proof guards.

The second run reached the real failure: **1 test / 1 failure**, 25.053 seconds, at 2026-09-11 16:29:51 UTC. All three authored identities and five input identities/timestamps passed. The SDK selected the same downstream work `sha256:a779025e685336968a7d73387e33bfc0685636e1f6132c5f158519d8a931cd7e`; both its frozen successor and the consumer planner's successor were absent, while the actual invocation capture required `sha256:91882f797682ad35a36981c22c402b309e39d58a1c1280c53bbb367d74f2616a`. Execution then threw the unchanged omitted-successor guard. This is library evidence, not an inference from an HTTP timeout.

The complete negative source/XML archive is `/Users/kamil/Documents/Projects/Blue/rooted-focused-followup-evidence.jtpYIX/library-ring-chord-negative-02.tar.gz`, SHA-256 `e55f8e0707ddaf99861e6820a54b59f75bd89f38567919c266f974152ad19548`. The first passing candidate archive in the same directory is `library-ring-chord-candidate-01.tar.gz`, SHA-256 `a5a42394a34631bf6049577272e1769b768e35ba82d0fbead4f9e028a075891d`; its fresh XML contains only the original method, at 2026-09-11 16:41:58 UTC. Older HTML owner directories in that archive are not additional test passes.

## Private repair and why it belongs in Coordination

The planner and capturer selected different source contexts. A live or managed publication plans its next retained work before the transaction installs the newly produced root view. The planner therefore lacked the exact co-owned representation prefix that the subsequent invocation capture correctly used. Merely reading the current source head would substitute ambient progress for the frozen logical boundary; deleting the capturer guard would accept incomplete work authority.

Both publishers now supply the same exact result-derived `RootedDocumentView` to the next-work planner and to the transaction's staged view. `ManagedRepresentationHistory` shares its existing source selection between consumer planning and root capture. Its private staged context requires the exact producing result and processing boundary, the complete derived owner set, exact published body identities, and each owner's numbered receipt epoch (or its unchanged retained epoch where no revision is issued). The durable publication prefix is validated first; only the exact staged producer invocation may extend it. No arbitrary endpoint-equal view, later independent head or invented owner epoch becomes source authority. The initial pending-tail lookup now uses the consumer context as well.

The production delta is confined to four internal classes: `ManagedRepresentationHistory`, `ManagedCatchUpPlanner`, `ManagedEpochApplicationExecutor` and `ContractsClosureAdapter` (**101 added / 31 deleted lines**). There is no public API, schema, gas tariff, budget, logical cutoff or spec change. The original successor and stale-capture guards remain intact. The normative basis is rooted processing RCP-CAUSE-01/02 (logical-boundary anchor and explicit same-epoch receipt order), RCP-OWN-03/04 (derived publication ownership, retained root-local evidence and no invented unchanged-owner epoch), and RCP-CAUSE-03 (physical schedule cannot change the semantic kind).

The first passing method also exercised wrong-result, missing/extra owner, wrong body, same-body/wrong-epoch staged-context negatives and rejection of the obsolete capture after publication. It consumed the exact frozen representation target without chasing reconciliation's later heads. The final store restart preserved all ordered managed receipt identities and public READY heads; attachment left the source event occurrence data unchanged. Newly added checks require all three actual session statuses to be READY, and each current epoch, BlueId and exact body to equal its public READY snapshot before and after restart. They also reassert the complete source event occurrence data after restart. These additions now pass in the combined gate. The second method additionally restarts immediately before C7 and compares the exact selected work and successor before continuing; it passes in that same run. These controls do not claim an exhaustive independent full receipt transport or full Python ring/chord/reconnect acceptance.

## Distinction from the separate FULL_HISTORY repair

The separate draft in `rooted-full-history-frontier-fix` changes static `ADMIT_CLOSURE` source-position retention in `ContractsClosureAdmissionAdapter` and `RootedAdmissionSources`. This ring starts three empty independent roots, then adds edges dynamically. Its failing boundary is the fifth real Timeline entry at 1800000000000005, not the FULL_HISTORY beginning sentinel. The omitted-successor branch differs from the frozen source-publication equality failure that motivated that patch. There is no static existing-child source set in the initial three admissions. Therefore that patch is not assumed to repair this dynamic failure; any interaction must be demonstrated by the unchanged SDK reproduction.

## Combined gate and remaining acceptance

Both methods and explicit READY controls passed with all selected adjacent SDK controls: 44/44, zero failures/errors/skips, JDK 17 and one worker/fork, sealed mPVtoK Language/BEX/Catalog dependencies. Production/test architecture guards also pass. The [combined qualification](rooted-history-combined-qualification.md) records the source-bound archive and command separately from both original negative archives and the first single-method positive. Full MyOS product and HTTP acceptance remain required after a coherent candidate is exported; this SDK result does not replace them.
