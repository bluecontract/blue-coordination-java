# Contracts 1.0 and SDK current verification boundary

This tree prepares the `3.0.0-rc.12` bounded external-pilot candidate.
`BlueCoordination.inMemory()` uses the bundled Contracts 1.0 release manifest;
the older `CoordinationEngine` surface remains an advanced/legacy boundary.

All current release verification resolves Language `3.1.0-rc.32`, BEX
`1.1.0-rc.6`, and Repository `3.0.0-rc.22` from Maven Central. No sibling
composite, Maven Local, or file-staged dependency path participates.

## Current claim

The public SDK acceptance corpus covers authored ordinary and cyclic admission,
exact targeting, broadcasts, typed multi-closure results, append/drain parity,
built-JAR consumption, new-lineage `FROM_NOW` operation-result admission,
retained managed-epoch catch-up, and reference-transparent exact execution.
The Order-draft and five-occurrence/three-lineage cases, malformed evidence,
rollback, and deterministic retry are included.

```text
implementationConformanceClaimed = true
externalPilotReady = true
publicRcArtifactReady = true
retainedManagedEpochProfile.publicReleaseReady = true
productionReleaseReady = false
CONTRACTS10_CURRENT_PERFORMANCE_CLAIM: NONE
```

The claim is deliberately bounded. Imported known-epoch drafts and arbitrary
Timeline-provider import remain unsupported and fail closed; retained existing
lineages use the separate exact managed-epoch path. RC12 adds cold-reopen engine
and SDK storage adapters under host-owned atomic persistence and fencing. It
does not supply a database, an authoritative provider-completeness service,
Mandates, distributed scheduling, production operations, arbitrary-history
resource bounds, or a stable latency SLA.

## Evidence ownership

`verifyCurrentContractsDocumentation` derives current source integrity.
`releaseCheck` runs all four suites, public and artifact boundaries,
publication metadata, documentation, and extracted-source verification against
published dependencies. `verifyRcReadiness` adds exact rc.12 policy and graph
checks and writes fresh built-artifact hashes to
`build/reports/release/3.0.0-rc.12-readiness.json`.

The complete current release gate and focused rc.12 capability inventory
authorize the semantic bounded-pilot profile. Historical handoff receipts
remain bound to their original releases. Current artifact evidence is
regenerated by the release workflow.

The maintainability guardrails allow at most 327 production Java files,
87,765 production Java lines, and 96 public source types across
`blue.coordination.api` and `blue.coordination.sdk`. These are engineering
tripwires, not semantic or performance evidence.

The rc.1 Round 13 report, JSON, schemas, and provenance remain immutable
historical evidence. They are not compared with current source counts and do
not authorize rc.12.
