# 3.0.0-rc.1 Round 13 interim verification report

Evidence date: 2026-08-12.

## Evidence state

```text
ROUND13_INTERIM_VERIFICATION: PENDING_VERIFICATION
```

This is an honest `INTERIM` record for the
`ROUND13_PLAYGROUND_FIVE_OCCURRENCE` profile. It records source-derived
inventory, static worktree shape, and a manifest-bound 30+30 performance
campaign. The retained campaign completed and failed its overall latency
decision; it is stale after the published-dependency input change and cannot be
promoted. The complete fresh Java 17 and Java 21 published-artifact lanes
passed, as did all seven proof rows, all eight measured zero counters, all four
artifact builds, and extracted-source configuration, focused tests, and
published-artifact smoke. The worktree is dirty, `candidateCommit` is `null`,
and clean-candidate reruns, the final detached checksum, and a fresh passing
latency campaign remain incomplete. No earlier Round 12 execution is promoted.

The top-level execution binding is `WORKTREE_EXECUTION`: the fresh suite
outcomes and durations, proofs, structural evidence, artifacts, extracted-
archive checks, and campaign are bound to the exact current production-source
manifest. This is not a clean candidate identity or a final release result, and
it does not permit staging.

| Status | Result | Meaning |
| --- | --- | --- |
| `temporalArchitectureReady` | `PENDING_VERIFICATION` | Fresh worktree semantic and retry proofs passed; a clean-candidate final rerun is still required. |
| `inMemoryEngineWorkingReady` | `PENDING_VERIFICATION` | Complete fresh Java 17 and Java 21 worktree lanes passed; a clean-candidate final rerun is still required. |
| `playgroundCoreReady` | `PENDING_VERIFICATION` | All seven proofs and eight zero counters passed for this worktree; final clean-candidate evidence is still required. |
| `playgroundLatencyReady` | `PENDING_VERIFICATION` | The retained local-composite campaign failed and is stale after switching dependency/tooling bindings; a fresh published-artifact campaign is required. |
| `providerBackedReady` | `OUT_OF_SCOPE` | Provider adapters and provider-backed durability are outside this Playground profile. |
| `mandateAgentReady` | `OUT_OF_SCOPE` | Mandate-backed delegation is outside Round 13; unsupported Mandate entries must still fail closed. |
| `publicRcReady` | `BLOCKED` | A clean commit, final reruns and rebuild, passing latency, and detached archive verification remain incomplete. |

## Worktree binding and static shape

The snapshot starts from base commit
`699f019cb9fa1d484a070a5d46a2335fd1cd2dce`. Its production source is bound to
manifest
`d0dad91fe3c2d5627c3b1900543b2a82cc9ea323afbb85ad06256da4e2cf3124`.
The machine evidence records the exact sorted path-and-file-digest algorithm.
This reproducible dirty-worktree binding is not a clean candidate identity and
does not establish final candidate identity. The fresh Java lanes, proof and
structural results, artifact hashes, and extracted/focused archive checks are
recorded as `WORKTREE_EXECUTION` for this manifest. The retained campaign is
separately classified as historical because its dependency binding is stale.

| Static worktree shape | Actual | Limit | Result |
| --- | ---: | ---: | --- |
| Java production source files | 109 | 115 | `PASS` |
| Production source lines | 25,952 | 25,952 | `PASS` |
| Public API source types | 16 | 16 | `PASS` |

The production-line limit is deliberately and transparently revised from
25,700 to 25,952. The bounded delta covers the occurrence-path audit receipt,
closed retry-counter vocabulary and canonical mappings, real retry-monitor
producers, the bounded-drain timestamp-consumption fix, the attachment-
timestamp robustness guard, retained committed-input recovery evidence,
deterministic invalid-entry preflight and terminalization, and injective
binding identities. It adds one bounded production evidence type but no public
API type, architecture subsystem, or shape failure.

## Source-derived test inventory and fresh worktree execution

The inventory below is derived from current source annotations. Both complete
fresh published-artifact lanes executed every listed test with zero failures,
errors, or skips.

| Suite | Classes | Test methods | Failures | Errors | Skipped | Execution |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| Unit (`test`) | 33 | 231 | 0 | 0 | 0 | `PASS` |
| Integration (`integrationTest`) | 41 | 91 | 0 | 0 | 0 | `PASS` |
| Built-JAR consumer (`consumerTest`) | 1 | 6 | 0 | 0 | 0 | `PASS` |
| Scenarios (`scenarioTest`) | 10 | 14 | 0 | 0 | 0 | `PASS` |
| **Total** | **85** | **342** | **0** | **0** | **0** | **`PASS`** |

The refreshed preliminary lanes executed all four source sets with reruns using
the published-artifact default. Documentation and staging remain independently
blocked by the stale failing campaign and final-evidence requirements.

| Runtime | Required gate | Status | Duration |
| --- | --- | --- | ---: |
| Java 17 | `clean build`; isolated `scenarioTest --rerun-tasks`; then `releaseCheck`, all with `-PtestJavaVersion=17` | `PASS` | 882 s (14m42s) |
| Java 21 | `test integrationTest consumerTest scenarioTest --rerun-tasks`; then `releaseCheck`, all with `-PtestJavaVersion=21` | `PASS` | 870 s (14m30s) |

These are honest dirty-worktree preliminary results. A clean tested
implementation commit and complete clean-candidate reruns remain mandatory for
final evidence.

## Exact Round 13 proof rows

These are the seven mandatory proof bindings. The six JUnit/built-JAR proofs
passed against the current manifest in the fresh worktree lanes. One fresh
published-artifact `round13PlaygroundSample` separately exercised the host-
versus-frozen invariant and exact structural counts; its latency decision
remains pending a new campaign. Final evidence still requires their clean-
candidate rerun.

| Proof | Source | Binding | Status |
| --- | --- | --- | --- |
| `five-occurrence-three-session-reuse` | `scenarioTest` | `PlaygroundFiveOccurrenceInitializationTest.fiveOccurrencesReuseThreeSessionsAndForwardFiveInitializationEvents` | `PASS` |
| `five-occurrence-five-parent-events` | `scenarioTest` | `PlaygroundFiveOccurrenceInitializationTest.fiveOccurrencesReuseThreeSessionsAndForwardFiveInitializationEvents` | `PASS` |
| `five-occurrence-canonical-path-order` | `scenarioTest` | `PlaygroundFiveOccurrenceDeterminismScenarioTest.reverseRequestInsertionOrderPreservesBlueIdsHistoriesAndEvents` | `PASS` |
| `five-occurrence-retry-exactly-once` | `integrationTest` | `PlaygroundFiveOccurrenceRetryTest.retryCompletesFiveOccurrenceFanoutWithoutReinitializingChildren` | `PASS` |
| `five-occurrence-public-JAR-consumer` | `consumerTest` | `PublishedArtifactConsumerTest.fiveEmbeddedOccurrencesReuseThreeManagedDocuments` | `PASS` |
| `five-occurrence-no-generic-fragmentation` | `scenarioTest` | `PlaygroundFiveOccurrenceInitializationTest.fiveOccurrencesReuseThreeSessionsAndForwardFiveInitializationEvents` | `PASS` |
| `five-occurrence-host-versus-frozen-time` | `performance` | `round13PlaygroundSample` and the interleaved campaign harness | `PASS` |

The required final result is five embedding occurrences, three unique managed
child sessions, three initialization calls, five parent PROCESS applications,
five parent-visible initialization-event occurrences, canonical path order,
and no generic fragmentation. The JUnit and built-JAR proofs passed in both
fresh Java lanes. The retained historical campaign records the required counts
and exact host/frozen decomposition, but a new published-artifact campaign is
required for the current latency decision.

## Structural evidence

The canonical metric vocabulary contains eight required counters. The fresh
worktree lanes passed the canonical structural assertions, and the fresh
published-artifact sample emitted all eight at zero. The retained historical
campaign also produced every counter at measured zero in all 30 candidate rows,
and its frozen importer rejects an absent counter rather than treating it as
zero.

| Required zero counter | Value | Status |
| --- | ---: | --- |
| `REQUEST_FRAGMENTS` | 0 | `PASS` |
| `TIMELINE_ENTRY_FRAGMENTS` | 0 | `PASS` |
| `ORDINARY_NODE_FRAGMENTS` | 0 | `PASS` |
| `FULL_ENVIRONMENT_SCANS` | 0 | `PASS` |
| `SOURCE_REPLAYS_PER_PARENT` | 0 | `PASS` |
| `POST_PROCESS_FULL_PROJECTIONS` | 0 | `PASS` |
| `PARENT_PROCESS_RERUNS_ON_GRAPH_RETRY` | 0 | `PASS` |
| `CHILD_PROCESS_RERUNS_ON_PARENT_RETRY` | 0 | `PASS` |

## Superseded same-machine performance campaign

The retained local-composite decision campaign is historical diagnostic
evidence and no longer decides the current published-artifact candidate. It is
`SAME_MACHINE_INTERLEAVED_AB`: exact
baseline `Archive.zip` (SHA-256
`2f303afd9435937145ce7fe09eb655ff1a05ee6ed7484faea62d93544a3ccf3e`)
versus the manifest-bound candidate worktree, three warmups, and exactly 30
measured rows per side in odd A/B, even B/A order. Every row used the same Java
17 runtime identity and entry Blue ID. Campaign ID is
`85fd124f-3235-4874-9143-fc379b37b924`. The retained raw bytes, observed
completion ledger, pre/post bindings, fixtures, locks, wrapper, composite
dependency manifests, baseline archive, and then-current source/harness manifests
are independently revalidated by the importer. The opt-in campaign remains
outside `releaseCheck` and the default build loop.

The exact archive predates the two retry-counter producers, so its rows mark
those counters unavailable rather than inventing zeros. Every candidate row
produced both retry counters at zero. Every row also proves total is at least
frozen time and `coordinationHostNanos` equals total minus frozen time, with six
frozen process calls. The frozen importer validates that retained evidence; the
fresh `round13PlaygroundSample` lane separately exercises the same invariant.

Canonical outputs are
[`five-occurrence-runtime.md`](evidence/round13/five-occurrence-runtime.md),
[`five-occurrence-runtime.json`](evidence/round13/five-occurrence-runtime.json),
and [`runtime-provenance.json`](evidence/round13/runtime-provenance.json).

### All measured distributions

Values are milliseconds. Each distribution is recomputed from the 30 retained
raw rows for that variant.

| Variant | Span | p50 ms | p95 ms | max ms |
| --- | --- | ---: | ---: | ---: |
| baseline | requestPreparationNanos | 96.757125 | 109.855584 | 122.837208 |
| baseline | appendNanos | 16.782541 | 19.348042 | 20.221625 |
| baseline | routeNanos | 0.062375 | 0.075208 | 0.076375 |
| baseline | embeddedInputPreparationNanos | 34.646542 | 37.111500 | 37.795750 |
| baseline | hostBeforeFrozenNanos | 39.582125 | 41.871376 | 44.445083 |
| baseline | processFrozenNanos | 2795.280292 | 2897.255416 | 2971.856500 |
| baseline | initializationFrozenNanos | 282.107709 | 307.495041 | 332.215668 |
| baseline | frozenNanos | 3079.054084 | 3187.656792 | 3252.091709 |
| baseline | hostAfterFrozenNanos | 405.517207 | 425.483790 | 425.643793 |
| baseline | initializationHostBeforeFrozenNanos | 107.375959 | 117.220625 | 121.929334 |
| baseline | initializationHostAfterFrozenNanos | 104.437751 | 116.218292 | 116.446376 |
| baseline | graphPublicationNanos | 554.138959 | 609.396334 | 632.298751 |
| baseline | coordinationHostNanos | 841.987252 | 893.884918 | 901.694875 |
| baseline | totalNanos | 3922.524333 | 4073.318000 | 4093.335417 |
| candidate | requestPreparationNanos | 96.784375 | 100.347792 | 101.985625 |
| candidate | appendNanos | 16.881958 | 18.680667 | 32.729000 |
| candidate | routeNanos | 0.061625 | 0.097583 | 0.137333 |
| candidate | embeddedInputPreparationNanos | 34.757083 | 36.940292 | 37.382377 |
| candidate | hostBeforeFrozenNanos | 39.558458 | 42.090460 | 45.703667 |
| candidate | processFrozenNanos | 2783.524999 | 2833.506916 | 2906.517667 |
| candidate | initializationFrozenNanos | 281.715751 | 303.047416 | 304.281708 |
| candidate | frozenNanos | 3065.391458 | 3132.828916 | 3210.799375 |
| candidate | hostAfterFrozenNanos | 402.915208 | 413.814374 | 423.630876 |
| candidate | initializationHostBeforeFrozenNanos | 108.290333 | 112.242209 | 119.242000 |
| candidate | initializationHostAfterFrozenNanos | 104.654584 | 109.720083 | 110.207584 |
| candidate | graphPublicationNanos | 552.389793 | 583.484999 | 622.061500 |
| candidate | coordinationHostNanos | 843.715582 | 872.356126 | 898.613917 |
| candidate | totalNanos | 3907.544583 | 3998.664166 | 4109.413292 |

### Candidate gate decision

| Gate | Candidate p95 ms | Preferred max ms | Preferred | Hard max ms | Hard |
| --- | ---: | ---: | --- | ---: | --- |
| Append | 18.680667 | 0.250000 | `MISS` | 1.000000 | `FAIL` |
| Route | 0.097583 | 0.050000 | `MISS` | 0.200000 | `PASS` |
| Coordination host | 872.356126 | 120.000000 | `MISS` | 250.000000 | `FAIL` |
| Total | 3998.664166 | 2500.000000 | `MISS` | 4000.000000 | `PASS` |

The superseded campaign decision is **`FAIL`**. Append and coordination-host
miss their hard limits; route and total pass hard, while all four metrics miss
preferred. Those measurements remain useful diagnostics, but dependency and
tooling bindings have changed. Current latency readiness is therefore pending a
new published-artifact campaign; the old failure is not relabeled.

Round13B was deliberately not applied. Candidate embedded-input preparation
p95 is 36.940292 ms versus 872.356126 ms coordination-host p95—about 4.2%.
The optional cache targets too little of the measured host span to address the
append or coordination-host hard misses.

> **NO_POSTFLIGHT_TOOLING_CHANGES:** the tracked importer and receipt generator
> are the same frozen file, and the runner, current `Round12NbaEvidence`,
> candidate execution and harness surfaces, fixtures, locks, wrapper,
> composites, baseline archive and project, Java runtime, and physical host
> must match their exact preflight bindings through postflight, receipt
> generation, and independent verification. No substitution was permitted.
> The importer and generator SHA-256 is
> `e33c17c94f8cec762980375005e7632e1a7cd05b378adbfd38376d2fe721bf50`;
> the runner SHA-256 is
> `996210570704b32caf3a3886044f8f1b34c69824c7ba4a0f4518fea2352c7b5d`.

The Round 12 performance record remains `HISTORICAL_ONLY` and does not decide
Round 13 readiness.

## Artifacts, source archive, and publication

Both fresh Java lanes produced byte-identical current-manifest artifacts:

| Artifact | SHA-256 | Status |
| --- | --- | --- |
| Main JAR | `af211bbd2a8ca50424f7a9b0ac0056705f5a3d6bafe0a8cd3a8d02e8b164f0ff` | `PASS` |
| Sources JAR | `6e23e4e21286a7394f8d4904c8ad01f811f14025648c360b5b68c52341857da8` | `PASS` |
| Javadoc JAR | `b6cc39927e4aabf945054100ab33c7fcf45589e78feed739e9b3a1351f37a6f3` | `PASS` |
| Test-fixtures JAR | `d4393f58502f81583792af01b50f5350d5f8cafa3a9ba4d9752f039661f9cccb` | `PASS` |

The source archive is configured to include authoritative `.cz.toml`, use
reproducible order, strip timestamps, reject duplicates, and exclude nested
ZIPs, build output, Gradle state, macOS metadata, JFR recordings, and heap
dumps. Extracted configuration, expanded focused tests, detached-sidecar
verification, and published-artifact smoke all passed. The source archive
contains this report, so its digest deliberately remains external and `null` in
tracked evidence rather than creating a self-referential hash. The generated
`.sha256` sidecar is authoritative; a clean-candidate rebuild and final detached
verification remain required.

Fresh `dependencyPreflight` passed in 30 seconds: Repository rc.21, BEX core
rc.3, BEX contracts rc.3, and their complete transitive graph resolved from
published repositories with version-conflict checking enabled. This removes
the former upstream artifact blocker; it does not satisfy the independent
performance or clean-candidate release gates.

## Finalization boundary

The fresh worktree lanes, proofs, counters, artifact hashes, and extracted-
archive checks are preliminary manifest-bound passes; they do not replace the
clean-candidate final requirements. This report may carry the Round 13 final-
pass marker only after one clean tested implementation commit supplies complete
Java 17 and Java 21 reruns, seven passing proof rows, eight measured zero
counters, a passing 30-sample same-machine campaign, final JAR hashes, a valid
detached source-archive checksum, clean extracted-archive evidence, and passing
publication evidence.
The tested implementation commit may be an ancestor of the later clean
evidence HEAD, provided the current main-source manifest is identical.
`stageRelease` must reject this `INTERIM`, dirty, pending record.

Round 13 claims only a sequential, synchronous, single-process, in-memory
Playground engine with a closed local Timeline journal. It does not claim
provider-backed durability, database or distributed processing, cross-process
exactly-once behavior, Mandate-backed authority, final public-RC readiness, or
a passing latency campaign.

Machine-readable evidence is in
[`3.0.0-rc.1-evidence.json`](3.0.0-rc.1-evidence.json), described by
[`round13-verification.schema.json`](round13-verification.schema.json). The
[`round12-verification.schema.json`](round12-verification.schema.json) remains
historical and does not describe the canonical Round 13 evidence.
