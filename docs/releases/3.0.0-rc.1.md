# 3.0.0-rc.1 Round 13 Playground five-occurrence profile

This candidate profile narrows release readiness to the single-process,
single-writer, synchronous, in-memory Playground engine. Its flagship scenario
attaches five Process Embedded occurrences that reuse three unique managed
documents, initialize each child once, and forward five parent-visible
initialization-event occurrences in canonical path order.

## Current status

The canonical [verification report](3.0.0-rc.1-test-report.md) and
[machine-readable evidence](3.0.0-rc.1-evidence.json) are a `FINAL` record for
`ROUND13_PLAYGROUND_FIVE_OCCURRENCE`. The exact policy mode is
`RC_WITH_KNOWN_PERFORMANCE_LIMITATION`, applies only to `3.0.0-rc.1`, requires
explicit release-workflow opt-in, and yields
`PASS_WITH_KNOWN_PERFORMANCE_LIMITATION`. It is not eligible for stable release.
Java 17 and Java 21 published-artifact lanes passed, as did all six non-
performance proof rows, all eight zero counters, all four artifact builds, and
extracted-source configuration, focused tests, and published-artifact smoke.
All non-performance release gates remain mandatory.

| Status | Current result |
| --- | --- |
| Temporal architecture | `PASS` |
| In-memory working engine | `PASS` |
| Playground core | `PASS` |
| Playground latency | `PASS_WITH_KNOWN_PERFORMANCE_LIMITATION` |
| Provider-backed engine | `OUT_OF_SCOPE` |
| Mandate-backed agent authority | `OUT_OF_SCOPE` |
| Public RC | `PASS_WITH_KNOWN_PERFORMANCE_LIMITATION` |

The current source-derived inventory is 342 tests in 85 classes: 231/33 unit,
91/41 integration, 6/1 built-JAR consumer, and 14/10 scenario tests. Both fresh
lanes passed all 342 tests with zero failures, errors, or skips: Java 17 in
882 seconds (14m42s) across the clean build, isolated scenario rerun, and
release check; Java 21 in 870 seconds (14m30s) across the four-suite rerun and
release check. Both used the published-artifact default. Staging independently
reruns the non-performance gate and requires explicit workflow opt-in to this
exact exception.
The static production shape is 109 Java files, 25,952 lines, and 16 public API
types.

The line budget is transparently revised from 25,700 to 25,952 for the bounded
occurrence-path audit receipt, closed retry-counter vocabulary and real retry
monitors, bounded-drain timestamp consumption, the attachment-timestamp guard,
retained committed-input recovery evidence, deterministic invalid-entry
preflight and terminalization, and injective binding identities. This adds one
bounded production evidence type but no public API type or new architecture
subsystem.

## Required Round 13 proof

Core evidence must pass the exact five-occurrence scenario, retry proof,
canonical insertion-order determinism proof, public-JAR consumer proof,
no-fragmentation proof, and host-versus-frozen-time proof. It must report five
embedding bindings, three child sessions, three child initializations, five
parent PROCESS applications, five forwarded initialization occurrences,
canonical application paths, and all eight required forbidden-work counters at
measured zero. The superseded local-composite campaign confirms its historical
row-level counts, host-versus-frozen decomposition, and all eight candidate
counters across 30 rows. All six non-performance proof rows and the structural
evidence passed in the candidate execution. The current published-artifact
performance proof and campaign remain `PENDING_VERIFICATION`; the RC-only policy
does not represent either as passing.

The retained historical performance decision used a same-machine interleaved A/B
comparison of the exact `Archive.zip` baseline and then-current local-composite
candidate worktree, with three warmups and 30 raw rows per side. The retained
[runtime Markdown](evidence/round13/five-occurrence-runtime.md),
[runtime JSON](evidence/round13/five-occurrence-runtime.json), and
[provenance JSON](evidence/round13/runtime-provenance.json) independently
recompute every distribution and gate. Campaign
`85fd124f-3235-4874-9143-fc379b37b924` is **`FAIL`**: append p95 is
18.680667 ms against a 1.000000 ms hard limit and coordination-host p95 is
872.356126 ms against 250.000000 ms. Route p95 is 0.097583 ms and total p95 is
3998.664166 ms; both pass hard, while all four metrics miss preferred.

The historical receipts stay byte-for-byte unchanged. The temporary
`Archive.zip` used to produce them is not a release input and is not required to
stage or publish this RC.

Round13B was not applied: embedded-input preparation p95 is 36.940292 ms versus
872.356126 ms coordination-host p95 (about 4.2%), so the optional cache would
not address the measured hard misses. Round 12 data remains historical only.

> **NO_POSTFLIGHT_TOOLING_CHANGES:** one frozen importer and receipt generator,
> runner, current `Round12NbaEvidence`, candidate execution and harness
> surfaces, fixtures, locks, wrapper, composites, baseline, Java runtime, and
> physical host were bound exactly from preflight through independent
> verification. No postflight substitution was permitted. Importer/generator
> SHA-256:
> `e33c17c94f8cec762980375005e7632e1a7cd05b378adbfd38376d2fe721bf50`;
> runner SHA-256:
> `996210570704b32caf3a3886044f8f1b34c69824c7ba4a0f4518fea2352c7b5d`.

## Release boundary

Canonical artifact bytes are produced on Ubuntu 24.04 with Eclipse Temurin
17.0.19+10, `x64`, and the Gradle build cache disabled. The Java 21 lane uses
the same pinned Java 17 compiler for production classes and Eclipse Temurin
21.0.11+10 for test execution. Both fresh lanes produced byte-identical
current-manifest artifacts:

| Artifact | SHA-256 | Status |
| --- | --- | --- |
| Main JAR | `af9299ff8945f97307a6324bbaf975090d1e60d1c70c55c3a6717268db682857` | `PASS` |
| Sources JAR | `76451fc105dace25d01dc76dfc0813b2ddb06b255eaea374b5186bde7c56bf96` | `PASS` |
| Javadoc JAR | `4b2833c8499a7603ac3c72057648a090391896c63e3781be08043bd5385ecf0a` | `PASS` |
| Test-fixtures JAR | `d4393f58502f81583792af01b50f5350d5f8cafa3a9ba4d9752f039661f9cccb` | `PASS` |

Extracted configuration, the expanded focused tests, detached-sidecar
verification, and published-artifact archive smoke passed. Because the source
archive contains this release note, the archive digest stays external and
`null` in tracked evidence instead of becoming self-referential. The generated
`.sha256` sidecar is authoritative; staging rebuilds the archive and verifies
the generated sidecar before publication.

RC staging requires a tested implementation commit, Java 17 and Java 21 gates,
rebuilt and hashed JARs, a reproducible source archive whose detached checksum
sidecar validates and which passes the focused Round 13 tests after clean
extraction, and published-artifact smoke evidence. The tested implementation
commit may be an ancestor of the clean evidence HEAD when the current production-
source manifest still matches. Fresh
`dependencyPreflight` passed in 30 seconds with Repository rc.21, BEX core rc.3,
BEX contracts rc.3, and the complete conflict-checked transitive graph resolved
from published repositories. Fresh extracted-archive configuration, focused
tests, and published-artifact smoke also passed; local-composite execution was
not substituted for this evidence.

`stageRelease` accepts this policy only when the workflow supplies the explicit
3.0.0-rc.1 opt-in and every non-performance gate passes. Direct local staging,
other RC versions, and stable releases remain fail-closed. Performance
remediation and a passing campaign are mandatory before stable.
Provider-backed durability, databases, distributed processing, cross-process
exactly-once behavior, and Mandate-backed delegated authority remain explicit
non-claims for this Playground profile. Unsupported Mandate-backed entries must
still be rejected rather than emulated.
