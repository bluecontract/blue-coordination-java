# 3.0.0-rc.1 Round 13 Playground five-occurrence profile

This candidate profile narrows release readiness to the single-process,
single-writer, synchronous, in-memory Playground engine. Its flagship scenario
attaches five Process Embedded occurrences that reuse three unique managed
documents, initialize each child once, and forward five parent-visible
initialization-event occurrences in canonical path order.

## Current status

The canonical [verification report](3.0.0-rc.1-test-report.md) and
[machine-readable evidence](3.0.0-rc.1-evidence.json) are an `INTERIM`, dirty-
worktree record for `ROUND13_PLAYGROUND_FIVE_OCCURRENCE`. They retain static
source inventory and shape plus a manifest-bound 30+30 performance campaign.
The fresh hardened campaign completed with an overall `FAIL`. Current-manifest
Java 17 and Java 21 local-composite lanes passed, as did all seven proof rows,
all eight zero counters, all four artifact builds, and extracted-source
configuration and focused smoke. The top-level execution binding is
`WORKTREE_EXECUTION`; the worktree remains dirty, `candidateCommit` is `null`,
and clean-candidate reruns, the final detached checksum, published-artifact
smoke, and a passing latency campaign remain incomplete. Nothing is relabeled
as clean-candidate or final evidence.

| Status | Current result |
| --- | --- |
| Temporal architecture | `PENDING_VERIFICATION` |
| In-memory working engine | `PENDING_VERIFICATION` |
| Playground core | `PENDING_VERIFICATION` |
| Playground latency | `FAIL` |
| Provider-backed engine | `OUT_OF_SCOPE` |
| Mandate-backed agent authority | `OUT_OF_SCOPE` |
| Public RC | `BLOCKED` |

The current source-derived inventory is 342 tests in 85 classes: 231/33 unit,
91/41 integration, 6/1 built-JAR consumer, and 14/10 scenario tests. Both fresh
lanes passed all 342 tests with zero failures, errors, or skips: Java 17 in
912 seconds (15m12s) and Java 21 in 877 seconds (14m37s). Each used
`releaseCheck -x verifyDocumentation --rerun-tasks` in local-composite mode;
documentation verification was intentionally excluded while this canonical
record was being updated. The static production shape is 109 Java files,
25,952 lines, and 16 public API types.

The line budget is transparently revised from 25,700 to 25,952 for the bounded
occurrence-path audit receipt, closed retry-counter vocabulary and real retry
monitors, bounded-drain timestamp consumption, the attachment-timestamp guard,
retained committed-input recovery evidence, deterministic invalid-entry
preflight and terminalization, and injective binding identities. This adds one
bounded production evidence type but no public API type or new architecture
subsystem.

## Required Round 13 proof

Final evidence must pass the exact five-occurrence scenario, retry proof,
canonical insertion-order determinism proof, public-JAR consumer proof,
no-fragmentation proof, and host-versus-frozen-time proof. It must report five
embedding bindings, three child sessions, three child initializations, five
parent PROCESS applications, five forwarded initialization occurrences,
canonical application paths, and all eight required forbidden-work counters at
measured zero. The fresh campaign independently confirms its row-level counts,
host-versus-frozen decomposition, and all eight candidate counters across 30
rows. All seven canonical proof rows and the structural evidence also passed in
the fresh worktree execution. A clean-candidate rerun remains required before
final readiness.

The completed performance decision used a same-machine interleaved A/B
comparison of the exact `Archive.zip` baseline and candidate worktree, with
three warmups and 30 raw rows per side. The retained
[runtime Markdown](evidence/round13/five-occurrence-runtime.md),
[runtime JSON](evidence/round13/five-occurrence-runtime.json), and
[provenance JSON](evidence/round13/runtime-provenance.json) independently
recompute every distribution and gate. Campaign
`85fd124f-3235-4874-9143-fc379b37b924` is **`FAIL`**: append p95 is
18.680667 ms against a 1.000000 ms hard limit and coordination-host p95 is
872.356126 ms against 250.000000 ms. Route p95 is 0.097583 ms and total p95 is
3998.664166 ms; both pass hard, while all four metrics miss preferred.

Round13B was not applied: embedded-input preparation p95 is 36.940292 ms versus
872.356126 ms coordination-host p95 (about 4.2%), so the optional cache would
not address the measured hard misses. Round 12 data remains historical only.

> **NO_POSTFLIGHT_TOOLING_CHANGES:** one frozen importer and receipt generator,
> runner, current `Round12NbaEvidence`, candidate execution and harness
> surfaces, fixtures, locks, wrapper, composites, baseline, Java runtime, and
> physical host were bound exactly from preflight through independent
> verification. No postflight substitution was permitted. Importer/generator
> SHA-256:
> `e33c17c94f8cec762980375005e7632e1a7cd05b378adbfd38376d2fe721bf50`;
> runner SHA-256:
> `996210570704b32caf3a3886044f8f1b34c69824c7ba4a0f4518fea2352c7b5d`.

## Release boundary

Both fresh lanes produced byte-identical current-manifest artifacts:

| Artifact | SHA-256 | Status |
| --- | --- | --- |
| Main JAR | `af211bbd2a8ca50424f7a9b0ac0056705f5a3d6bafe0a8cd3a8d02e8b164f0ff` | `PASS` |
| Sources JAR | `6e23e4e21286a7394f8d4904c8ad01f811f14025648c360b5b68c52341857da8` | `PASS` |
| Javadoc JAR | `b6cc39927e4aabf945054100ab33c7fcf45589e78feed739e9b3a1351f37a6f3` | `PASS` |
| Test-fixtures JAR | `d4393f58502f81583792af01b50f5350d5f8cafa3a9ba4d9752f039661f9cccb` | `PASS` |

The source ZIP SHA-256 is
`48d8694cd2a88e78c1d242fa35dcd80131f54b9a41fae9950b0ca7f79abc88d1`;
the detached sidecar file SHA-256 is
`858222629e2a47f5f5217e28c0bd999562bb8a2b02f564eb19a6ded28b2debce`.
Extracted configuration and the expanded focused archive smoke passed.
Published-artifact smoke, the clean-candidate rebuild, and final detached
sidecar verification remain pending; the canonical checksum digest stays
external and `null` in tracked evidence.

Final readiness additionally requires a tested implementation commit, clean-
candidate Java 17 and Java 21 reruns, rebuilt and hashed JARs, a reproducible
source archive whose detached checksum sidecar validates and which passes the
focused Round 13 tests after clean extraction, and published-artifact smoke
evidence.
It also requires performance remediation followed by a campaign that passes
every hard gate; the current measured campaign cannot be promoted.
The tested implementation commit may be an ancestor of the clean evidence HEAD
when the current production-source manifest still matches. Fresh
`dependencyPreflight` reconfirmed `BLOCKED_UPSTREAM`:
`blue.bex:blue-bex-core:1.1.0-rc.3` and
`blue.bex:blue-bex-contracts:1.1.0-rc.3` are missing from Maven Central, and
`commons-codec` 1.15 versus 1.11 conflicts via `blue.repo` and `blue.language`.
Published archive smoke cannot proceed; local-composite execution cannot
substitute for published resolution.

`stageRelease` must continue to reject the current `INTERIM` evidence.
Provider-backed durability, databases, distributed processing, cross-process
exactly-once behavior, and Mandate-backed delegated authority remain explicit
non-claims for this Playground profile. Unsupported Mandate-backed entries must
still be rejected rather than emulated.
