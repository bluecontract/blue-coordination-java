# Reviewed local POC cyclic identity evidence

Date: 2026-09-06. This is a reviewed local expectation refresh, not a published specification/profile, production release, or formal all-catalog acceptance claim.

The 32 required scenarios and exact repeat counts remain unchanged. Every scenario executed before candidate capture; stored artifact equality assertions stayed enabled and failed until independent review. Two captures produced byte-identical compact JSON. The second also retained every admitted gas entry, counter quantity/subtotal, original gas-trace identity, total, and exact repeat comparison.

## Approval-bound candidates

The independent reviewer and owning agent approved these exact bytes after reviewing behavior, topology, and gas attribution:

| File | SHA-256 |
|---|---|
| `full-lifecycle-admission-identities.json` | `cde271d72413568e8197ca72aa0ae31a6c55be86bb96de12ee5bfca61ae7053c` |
| `full-lifecycle-admission-identities.md` | `c2d07de2caa815130025941b4184755a1f5398af6341fb17390242cc9a365714` |
| `gas-trace-review.json` | `455a5de8f4d8aec23662f9b1e9d2c7a48421d882a895fa20712aa2428f1a4761` |

The raw sidecar, candidate files, actual worker classpath, command and JUnit evidence are retained under `build/readiness-evidence/coordination-final-affected-20260906-pECpT9/`. The sidecar independently binds its compact artifact SHA. It is diagnostic evidence, not runtime or transport authority.

## What was checked

- All resulting document/component arrays, graph changes, subscription deltas and checkpoint writes are exactly unchanged. Public events differ only in dependent occurrence identities. Active occurrence rows are unchanged; inactive rows retain the exact selected view at retirement instead of following an ambient later head, with corresponding dependent identities/inventory generations.
- All 29 non-loop gas deltas are accounted for by recorded observation/event-route work and placement-reference updates. No unexplained residual cost remains. Representation variants and required fresh-engine repeats remain exactly equal.
- Partial detach changes `1545 -> 1627`: 32 route gas plus 50 placement gas. The latter comprises quantity 1 at sequence 174 for B and quantity 4 at sequence 204 for A's SCC.
- Four-member split changes `1375 -> 1441`: 36 route gas plus 30 placement gas, quantity 3 at sequence 248 for C's SCC.
- Both intentional loop traces retain exact old work prefixes. Completed workflow counts are 728 and 672. Per-work gas is unchanged except the initial pre-detach work `547 -> 563`, explained by 16 patch-route gas. The final totals are 99999 and 99996; the next invocation-owned dequeue costs 10 with remaining 1 and 4. No executed work occurrence is fabricated for that invocation-owned rejection. Full rollback, actual loop entry, no effects and fresh-run identity/trace parity remain asserted.
- The one package-private bounded initialization scenario is explicitly `CAPABILITY_FAILURE`, `UnsupportedRuntimeRole`, `INITIALIZATION_EVENT_QUEUE_REQUIRED`, not a semantic runtime fatal. Its matching invocation, incomplete diagnostic evidence, unchanged closure, no commit/publication, no durable documents and no effects are required by the validator. No other incomplete scenario is accepted.

No business input, required scenario, ordering assertion, rollback assertion, or production runtime rule was changed by this artifact refresh. The capture-only full-trace sidecar is inactive in normal artifact validation.
