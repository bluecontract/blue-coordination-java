# Event-only retained history

The real SDK reproduced `Historical occurrence cursor is ahead of source` when a source emitted two events without a body mutation at order100 and a parent later attached its saved initialized BlueId at200. The source processor result retained epoch0 while Coordination atomically stored its event receipt as revision1, as Contracts §5.7.1 permits. The stored rooted view had lost that second coordinate. Merely changing the captured head correctly failed complete historical-witness verification because its accompanying snapshot still named epoch0.

Rooted views now retain actual publication heads derived inside MultiDocumentPublicationTransaction from the fully validated replacement sessions. The original processor result and complete historical witness roles remain immutable. A separate Contracts-derived retained snapshot carries those host coordinates into subsequent capture/read expansion; it accepts exactly the derived owners and at most the one permitted host increment. DocumentSession independently requires the exact retained head before installing the view. Historical attachment still selects the retained view before its frozen logical boundary, not the latest independent source head.

Validation:531Contracts core tests and strict Javadoc passed on Language5b39a5ec. Coordination30focused tests passed with no failures/errors/skips, plus both public API boundaries and strict Javadoc. Coverage includes two event-only SDK sequences, actual wrong/missing-position and exact-head negatives, frozen cutoffs, saved-original journal ring, multi-admission, source-reference negatives, three gas/rollback/store-failure/restart tests, and birth publication. The repeated source positions100/150 catch up at attachment200; independently published500 remains LIVE. Restarts between applications and afterward preserve all source receipts and prevent duplicate effects. The unchanged shared-gas boundary and fault-cut tests passed. Evidence is retained in the integration campaign's rooted-retained-position-adjacent-boundaries log/XML.

This is a development repair. The public MyOS replay/fresh-JVM tests, literal RUN016 qualification and final release gates remain required. No specification, tariff, predecessor rule, retained receipt, production footprint cap or startup allowance is changed.
