# Combined history corrections — focused qualification

`focused-library-history-combined-02` passes **44/44 tests** in eleven owners,
with zero failures, errors or skipped tests. JUnit total time is 577.923 seconds;
Gradle completed successfully in 9 minutes 47 seconds. Both production-shape
and test-architecture guards pass. This is source-local SDK qualification, not
sealed-artifact MyOS acceptance or release readiness.

| Owner | Cases |
| --- | ---: |
| RootedBorrowedCycleReadinessTest | 6 |
| RootedFullHistoryAdmissionFrontierTest | 6 |
| RootedRingChordSuccessorReproductionTest | 2 |
| RootedLocalHistoryRecoveryTest | 7 |
| RootedJoinEligibilityTest | 5 |
| RootedTerminalTailSdkBoundaryTest | 2 |
| RootedLaggingObserverReadinessTest | 1 |
| RootedStaticAdmissionCutoffTest | 4 |
| RootedMultipleHistoricalOccurrencesTest | 2 |
| RootedCheckpointRepresentationSafetyTest | 6 |
| RootedRetargetInputTest | 3 |

All six Commerce controls now pass together, including real adapter G-1/G/G+1
publication/rollback, genuinely older selections and a genuine same-epoch
checkpoint-only local position. Both ring/chord methods pass with explicit
all-three-READY checks, exact current/READY epoch/BlueId/body equality, staged
proof negatives, stale-capture rejection, frozen terminal target and restart
before that target. The six FULL_HISTORY reference/gas controls and all selected
adjacent history/retarget guards pass on the same unchanged source bytes.

The run uses JDK 17 and the original immutable Language `2ce3e66b`, BEX
`ab72af14`, Catalog `0b68744` artifacts. Coordination is the source-local
post-d038 candidate; its dirty source is not labelled a d038 release. All 28
modified/new files were hashed while the run was active and matched after it;
the source was not edited during execution. The later status-document edits
are not part of that source archive. Run 01 executed no tests: Gradle rejected
the incorrectly placed `--tests` options. Its failure log is preserved too.

Archive: `rooted-focused-followup-evidence.jtpYIX/library-history-combined-02.tar.gz`.
SHA-256: `575658bae4e76834628e3ab38e3316b028995df58c395f7f58ce1a9451551768`.
It contains complete source, XML, HTML, both command logs, the corrected command
and source checksums. Fresh XML (eleven suites) is the executed-test authority.

The original full MyOS red results remain unchanged. Next: clean source commit,
new immutable Coordination export, exact MyOS binding, original failed public
owners (including the 54-program fresh-process restart), then complete product
and independent HTTP suites plus the remaining library release gates. The
eligibility memo remains conditional on its actual end-to-end performance
benefit. No library merge/release or persistence-POC restart is authorized here.
